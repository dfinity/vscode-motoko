describe('Program', () => {
    test('basic actor', () => {
        const source = `
            import a "a";
            import { x = y } "b";
            actor {
                public let x = 5;
            }
        `;

const prog = 

        expect();
    });
});
